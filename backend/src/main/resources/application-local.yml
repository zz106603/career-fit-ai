spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/career_fit}
    username: ${DB_USERNAME:career_fit}
    password: ${DB_PASSWORD}
  flyway:
    baseline-on-migrate: true
    baseline-version: 0
